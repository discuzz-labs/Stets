{
  "name": "veve",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "bin": {
    "veve": "./bin/index.js"
  },
  "scripts": {
    "postinstall": "echo 'Thanks for using stets \n https://github.com/discuzz-labs/stets'",
    "build:watch": "tsc -w",
    "build:core": "RUSTFLAGS='-C opt-level=s' wasm-pack build src/core --mode no-install --out-name wasm --out-dir ./ --target nodejs --no-pack --release"
  },
  "files": [
    "/dist"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.8.1",
    "typescript": "^5.6.3"
  }
}
